var A = "This is the first ", B = "and this is the second. ", C = "This third one just does whatever it wants."; //Variables are being assigned to strings
var A = A.fontcolor("Blue"), B = B.fontcolor("Green"), C = C.fontcolor("Red");//Variables are being assigned a color
document.write(A, B, C); //The webpage is writing out the variables
window.alert(3*3) //This is an expression showing up in an alert box, since alerts are expressive, it just makes sense.